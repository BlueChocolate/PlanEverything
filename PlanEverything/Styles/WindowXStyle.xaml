<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:pu="https://opensource.panuon.com/wpf-ui">

    <!--  此处用数据绑定 Effect 并在后台指定 AcrylicWindowXEffect 也可以，但是这样更优雅  -->
    <Style x:Key="ShellWindowStyle" TargetType="pu:WindowX">
        <Setter Property="Width" Value="800" />
        <Setter Property="Height" Value="600" />
        <Setter Property="MinWidth" Value="800" />
        <Setter Property="MinHeight" Value="450" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="Foreground" Value="{DynamicResource ShellForegroundBrush}" />
        <Setter Property="Background" Value="{DynamicResource ShellBackgroundBrush}" />
        <Setter Property="MaskBrush" Value="{DynamicResource MaskBackgroundBrush}" />
        <!--<Setter Property="pu:WindowX.Effect">
            <Setter.Value>
                <pu:AcrylicWindowXEffect AccentColor="{DynamicResource ShellBackgroundTransparentColor}" />
            </Setter.Value>
        </Setter>-->
    </Style>
</ResourceDictionary>