<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:pu="https://opensource.panuon.com/wpf-ui">
    <Style
        x:Key="IconToggleButtonStyle"
        BasedOn="{StaticResource {x:Static pu:StyleKeys.ToggleButtonStyle}}"
        TargetType="ToggleButton">
        <Setter Property="Width" Value="30" />
        <Setter Property="Height" Value="30" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{DynamicResource StandardForegroundBrush}" />
        <Setter Property="pu:ToggleButtonHelper.HoverForeground" Value="{DynamicResource PrimaryForegroundBrush}" />
        <Setter Property="pu:ToggleButtonHelper.ClickForeground" Value="{DynamicResource PrimaryForegroundBrush}" />
        <Setter Property="pu:ToggleButtonHelper.CheckedForeground" Value="{DynamicResource PrimaryForegroundBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>
</ResourceDictionary>